import hero from './hero';
import about from './about';
import project from './project';
import resume from './resume';
import { contactSchema, siteSettingsSchema } from './contact';

export const schemaTypes = [hero, about, project, resume, contactSchema, siteSettingsSchema];
